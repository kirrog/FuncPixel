from func.Function import Function
from typing import List

func_base = ("Const", "Cosine", "Exponent", "Logarithm", "Polynome", "Sinus")


def read_function(data: str) -> Function:
    parts = data.split(sep=' ')


def split_by_hierarchy(lists: List[str]):  # one place
    pass
